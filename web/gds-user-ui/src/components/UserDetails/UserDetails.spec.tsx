import { render } from 'utils/test-utils';
import UserDetails from '.';

describe('<UserDetails />', () => {
  it('should ', () => {
    render(<UserDetails userId="" createdDate="" status={''} permissions={''} lastLogin={''} />);
  });
});
